# 复制为 .env 后改成本机/服务器真实值。不要把 .env 提交到 Git。
# dev / prod 靠「每台机器自己的 .env」或部署环境变量，不靠仓库里多份环境文件（除非你刻意用 APP_ENV 选文件）。

# --- PostgreSQL（与 libpq 习惯一致，psycopg 可直接用）---
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=core_db
PGUSER=db_admin
PGPASSWORD=change_me

# 可选：仅用于日志/选配置 (dev|prod|staging)
# APP_ENV=dev

# --- Jina GGUF：Q8_0 单文件，绝对路径 ---
# ISKRA_GGUF_PATH=/abs/path/v5-small-retrieval-Q8_0.gguf

# --- llama-cpp：开发、生产都为 CPU，固定为 0 ---
ISKRA_N_GPU_LAYERS=0

# --- LlamaIndex 大模型 HTTP 客户端（OpenAI 兼容协议；任选厂商）---
# API_KEY=sk-...
# BASE_URL=https://api.openai.com/v1
# MODEL=gpt-4o-mini
#
# 当 MODEL 不在 OpenAI 官方名单时建议设置：
# CONTEXT_WINDOW=131072
# LLM_USE_CHAT_COMPLETIONS=true
# LLM_FUNCTION_CALLING=true